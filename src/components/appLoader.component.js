// Import assets
import { Loader } from 'assets'

export default function AppLoader() {
    return (
        <section className='AppLoader'>
            <Loader className='AppLoader-loader' />
        </section>
    )
}
